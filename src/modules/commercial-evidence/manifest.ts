import type { FrontendModuleManifest } from "@/platform/module-registry";

export const COMMERCIAL_EVIDENCE_MODULE_MANIFEST: FrontendModuleManifest = {
  key: "commercial-evidence",
  workspace: "crm",
  routes: [],
};
