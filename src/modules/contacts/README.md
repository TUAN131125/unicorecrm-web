# Contacts module

The Contacts module owns Contact records, repository state, Contact-specific domain rules, queries, commands, and React presentation surfaces.

Cross-module workflows must use the module public API from `@/modules/contacts` rather than importing Contact internals.
