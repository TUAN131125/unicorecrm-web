# Deals Module

Deal owns opportunity records, pipeline stage configuration, application commands, queries, and Deal presentation surfaces.

Cross-module consumers must import only from `@/modules/deals`.
