// @generated by scripts/api/generate-openapi-client.mjs. Do not edit by hand.
// OpenAPI 3.1.0; contract 0.23.20-contract.0; SHA-256 5fdb408b986cad455e09a4b385cf8666c9cbefa09af25f91119adb8a039bff22
import type { HttpClient } from "@/platform/api/client";

export const OPENAPI_CONTRACT_VERSION = "0.23.20-contract.0" as const;
export const OPENAPI_SPEC_SHA256 = "5fdb408b986cad455e09a4b385cf8666c9cbefa09af25f91119adb8a039bff22" as const;
export const OPENAPI_SERVER_PATH = "/v1" as const;

export interface AccessGovernanceRequestOptions {
  signal?: AbortSignal;
  idempotencyKey?: string;
  expectedVersion?: number;
  retry?: "never" | "idempotent";
}

export type EntityId = string;

export type WorkspaceId = string;

export type CorrelationId = string;

export type IdempotencyKey = string;

export type ResourceVersion = number;

export type UtcDateTime = string;

export type AccessDataScope = "OWN" | "TEAM" | "WORKSPACE" | "CUSTOM";

export interface AccessDataScopePolicyDocument {
  policyId: EntityId;
  workspaceId: WorkspaceId;
  roleId: EntityId;
  resourceKey: string;
  scope: AccessDataScope;
  allowedOwnerIds?: Array<EntityId>;
}

export type AccessFieldAccess = "READ_WRITE" | "READ_ONLY" | "MASKED" | "HIDDEN";

export interface AccessFieldSecurityPolicyDocument {
  policyId: EntityId;
  workspaceId: WorkspaceId;
  roleId: EntityId;
  resourceKey: string;
  fieldKey: string;
  access: AccessFieldAccess;
}

export interface AccessMutationResponse {
  commandId: string;
  correlationId: CorrelationId;
  aggregateId: EntityId;
  aggregateType: string;
  version: ResourceVersion;
  occurredAt: UtcDateTime;
  outcome: "COMMITTED" | "REPLAYED";
  warnings?: Array<string>;
  emittedEventIds?: Array<string>;
  auditEvidenceIds?: Array<string>;
  result: WorkspaceAccessDirectoryDocument;
}

export type AccessProductSpace = "crm" | "studio" | "people";

export interface AccessRoleDataScopeInput {
  resourceKey: string;
  scope: AccessDataScope;
  allowedOwnerIds?: Array<EntityId>;
}

export interface AccessRoleDocument {
  roleId: EntityId;
  workspaceId: WorkspaceId;
  name: string;
  description?: string;
  sourceTemplateId?: string;
  isActive: boolean;
  capabilities: Array<string>;
  version: ResourceVersion;
}

export interface AccessRoleFieldSecurityInput {
  resourceKey: string;
  fieldKey: string;
  access: AccessFieldAccess;
}

export interface ArchiveAccessRoleRequest {
  reason?: string;
}

export interface AuthorizationContextDocument {
  workspaceId: WorkspaceId;
  membershipId: EntityId;
  memberId: EntityId;
  accountId: EntityId;
  roleIds: Array<EntityId>;
  roleTemplateIds: Array<string>;
  capabilities: Array<string>;
  productSpaces: Array<AccessProductSpace>;
  dataScopes: Array<AuthorizationDataScopeEntry>;
  fieldSecurity: Array<AuthorizationFieldAccessEntry>;
  evaluatedAt: UtcDateTime;
}

export interface AuthorizationDataScopeEntry {
  resourceKey: string;
  scope: AccessDataScope;
}

export interface AuthorizationFieldAccessEntry {
  resourceKey: string;
  fieldKey: string;
  access: AccessFieldAccess;
}

export interface ChangeWorkspaceMemberStatusRequest {
  status: "active" | "suspended";
  reason?: string;
}

export interface CreateAccessRoleRequest {
  name: string;
  description?: string;
  sourceTemplateId?: string;
  capabilities: Array<string>;
  dataScopes: Array<AccessRoleDataScopeInput>;
  fieldSecurity: Array<AccessRoleFieldSecurityInput>;
}

export interface EffectiveAccessDecisionReasonDocument {
  code: string;
  effect: "ALLOW" | "DENY" | "LIMIT";
  message?: string;
  source?: string;
}

export interface EffectiveRecordAccessDocument {
  workspaceId: WorkspaceId;
  resourceKey: string;
  recordId?: EntityId;
  canRead: boolean;
  canUpdate: boolean;
  canDelete: boolean;
  canExport: boolean;
  canApprove: boolean;
  allowedCommands: Array<string>;
  fieldAccess: Record<string, AccessFieldAccess>;
  decisionReasons: Array<EffectiveAccessDecisionReasonDocument>;
  evaluatedAt: UtcDateTime;
  authority: "backend";
}

export interface EvaluateEffectiveRecordAccessRequest {
  resourceKey: string;
  recordId?: EntityId;
  requestedCommands?: Array<string>;
  requestedFields?: Array<string>;
  includeExport?: boolean;
  includeApproval?: boolean;
}

export interface InviteWorkspaceMemberRequest {
  displayName: string;
  email: string;
  roleIds: Array<EntityId>;
  teamIds: Array<EntityId>;
}

export interface ProvisionWorkspaceMemberRequest {
  displayName: string;
  email: string;
  roleIds: Array<EntityId>;
  teamIds: Array<EntityId>;
}

export interface ReplaceAccessRoleRequest {
  name: string;
  description?: string;
  sourceTemplateId?: string;
  isActive: boolean;
  capabilities: Array<string>;
  dataScopes: Array<AccessRoleDataScopeInput>;
  fieldSecurity: Array<AccessRoleFieldSecurityInput>;
}

export interface ReplaceWorkspaceMemberAccessRequest {
  roleIds: Array<EntityId>;
  teamIds: Array<EntityId>;
}

export interface RoleAssignmentDocument {
  assignmentId: EntityId;
  workspaceId: WorkspaceId;
  membershipId: EntityId;
  roleId: EntityId;
}

export interface RotateManagedMemberPasswordRequest {
  reason?: string;
}

export interface WorkspaceAccessDirectoryDocument {
  workspaceId: WorkspaceId;
  revision: ResourceVersion;
  generatedAt: UtcDateTime;
  members: Array<WorkspaceAccessMemberDocument>;
  memberProfiles: Array<WorkspaceMemberProfileDocument>;
  invitations: Array<WorkspaceInvitationDocument>;
  roles: Array<AccessRoleDocument>;
  assignments: Array<RoleAssignmentDocument>;
  dataScopes: Array<AccessDataScopePolicyDocument>;
  fieldSecurity: Array<AccessFieldSecurityPolicyDocument>;
}

export interface WorkspaceAccessMemberDocument {
  membershipId: EntityId;
  accountId?: EntityId;
  memberId: EntityId;
  workspaceId: WorkspaceId;
  workspaceKey: string;
  name: string;
  status: WorkspaceMembershipStatus;
  logoText: string;
  teamIds: Array<EntityId>;
  roleIds: Array<EntityId>;
  source: "seed" | "invitation" | "direct_provisioning" | "external_identity";
  createdAt?: UtcDateTime;
  version: ResourceVersion;
}

export interface WorkspaceInvitationActionRequest {
  reason?: string;
}

export interface WorkspaceInvitationDocument {
  invitationId: EntityId;
  membershipId: EntityId;
  workspaceId: WorkspaceId;
  email: string;
  displayName: string;
  roleIds: Array<EntityId>;
  teamIds: Array<EntityId>;
  status: WorkspaceInvitationStatus;
  createdAt: UtcDateTime;
  lastSentAt: UtcDateTime;
  expiresAt: UtcDateTime;
  acceptedAt?: UtcDateTime;
  revokedAt?: UtcDateTime;
  version: ResourceVersion;
}

export type WorkspaceInvitationStatus = "PENDING" | "ACCEPTED" | "EXPIRED" | "REVOKED";

export interface WorkspaceMemberProfileDocument {
  memberId: EntityId;
  membershipId: EntityId;
  accountId?: EntityId;
  displayName: string;
  email?: string;
  accountSource: "seed" | "direct" | "invitation" | "external" | "unknown";
  accountStatus?: "ACTIVE" | "SUSPENDED";
  roleLabel?: string;
  provisionedAt?: UtcDateTime;
}

export type WorkspaceMembershipStatus = "active" | "suspended" | "invited";

export interface ArchiveAccessRoleQuery {
}

export interface ChangeWorkspaceMemberStatusQuery {
}

export interface CreateAccessRoleQuery {
}

export interface EvaluateEffectiveRecordAccessQuery {
}

export interface GetCurrentAuthorizationContextQuery {
}

export interface GetWorkspaceAccessDirectoryQuery {
}

export interface InviteWorkspaceMemberQuery {
}

export interface ProvisionWorkspaceMemberQuery {
}

export interface ReplaceAccessRoleQuery {
}

export interface ReplaceWorkspaceMemberAccessQuery {
}

export interface ResendWorkspaceInvitationQuery {
}

export interface RevokeWorkspaceInvitationQuery {
}

export interface RotateManagedMemberPasswordQuery {
}

export class AccessGovernanceApiClient {
  constructor(private readonly http: HttpClient) {}

  archiveAccessRole<TResponse = AccessMutationResponse, TBody extends object = ArchiveAccessRoleRequest>(roleId: EntityId, body: TBody, options: AccessGovernanceRequestOptions = {}): Promise<TResponse> {
    return this.http.request<TResponse, TBody>({
      operationId: "archiveAccessRole",
      method: "POST",
      path: `/access/roles/${encodeURIComponent(roleId)}/archive`,
      credentials: "include",
      body,
      ...(options.signal === undefined ? {} : { signal: options.signal }),
      ...(options.idempotencyKey === undefined ? {} : { idempotencyKey: options.idempotencyKey }),
      ...(options.expectedVersion === undefined ? {} : { expectedVersion: options.expectedVersion }),
      retry: options.retry ?? "never",
    });
  }

  changeWorkspaceMemberStatus<TResponse = AccessMutationResponse, TBody extends object = ChangeWorkspaceMemberStatusRequest>(membershipId: EntityId, body: TBody, options: AccessGovernanceRequestOptions = {}): Promise<TResponse> {
    return this.http.request<TResponse, TBody>({
      operationId: "changeWorkspaceMemberStatus",
      method: "POST",
      path: `/access/members/${encodeURIComponent(membershipId)}/status`,
      credentials: "include",
      body,
      ...(options.signal === undefined ? {} : { signal: options.signal }),
      ...(options.idempotencyKey === undefined ? {} : { idempotencyKey: options.idempotencyKey }),
      ...(options.expectedVersion === undefined ? {} : { expectedVersion: options.expectedVersion }),
      retry: options.retry ?? "never",
    });
  }

  createAccessRole<TResponse = AccessMutationResponse, TBody extends object = CreateAccessRoleRequest>(body: TBody, options: AccessGovernanceRequestOptions = {}): Promise<TResponse> {
    return this.http.request<TResponse, TBody>({
      operationId: "createAccessRole",
      method: "POST",
      path: "/access/roles",
      credentials: "include",
      body,
      ...(options.signal === undefined ? {} : { signal: options.signal }),
      ...(options.idempotencyKey === undefined ? {} : { idempotencyKey: options.idempotencyKey }),
      ...(options.expectedVersion === undefined ? {} : { expectedVersion: options.expectedVersion }),
      retry: options.retry ?? "never",
    });
  }

  evaluateEffectiveRecordAccess<TResponse = EffectiveRecordAccessDocument, TBody extends object = EvaluateEffectiveRecordAccessRequest>(body: TBody, options: AccessGovernanceRequestOptions = {}): Promise<TResponse> {
    return this.http.request<TResponse, TBody>({
      operationId: "evaluateEffectiveRecordAccess",
      method: "POST",
      path: "/access/records/evaluate",
      credentials: "include",
      body,
      ...(options.signal === undefined ? {} : { signal: options.signal }),
      ...(options.idempotencyKey === undefined ? {} : { idempotencyKey: options.idempotencyKey }),
      ...(options.expectedVersion === undefined ? {} : { expectedVersion: options.expectedVersion }),
      retry: options.retry ?? "never",
    });
  }

  getCurrentAuthorizationContext<TResponse = AuthorizationContextDocument>(query: GetCurrentAuthorizationContextQuery = {}, signal?: AbortSignal): Promise<TResponse> {
    return this.http.request<TResponse>({
      operationId: "getCurrentAuthorizationContext",
      method: "GET",
      path: "/access/context",
      credentials: "include",
      ...(signal === undefined ? {} : { signal }),
    });
  }

  getWorkspaceAccessDirectory<TResponse = WorkspaceAccessDirectoryDocument>(query: GetWorkspaceAccessDirectoryQuery = {}, signal?: AbortSignal): Promise<TResponse> {
    return this.http.request<TResponse>({
      operationId: "getWorkspaceAccessDirectory",
      method: "GET",
      path: "/access/directory",
      credentials: "include",
      ...(signal === undefined ? {} : { signal }),
    });
  }

  inviteWorkspaceMember<TResponse = AccessMutationResponse, TBody extends object = InviteWorkspaceMemberRequest>(body: TBody, options: AccessGovernanceRequestOptions = {}): Promise<TResponse> {
    return this.http.request<TResponse, TBody>({
      operationId: "inviteWorkspaceMember",
      method: "POST",
      path: "/access/invitations",
      credentials: "include",
      body,
      ...(options.signal === undefined ? {} : { signal: options.signal }),
      ...(options.idempotencyKey === undefined ? {} : { idempotencyKey: options.idempotencyKey }),
      ...(options.expectedVersion === undefined ? {} : { expectedVersion: options.expectedVersion }),
      retry: options.retry ?? "never",
    });
  }

  provisionWorkspaceMember<TResponse = AccessMutationResponse, TBody extends object = ProvisionWorkspaceMemberRequest>(body: TBody, options: AccessGovernanceRequestOptions = {}): Promise<TResponse> {
    return this.http.request<TResponse, TBody>({
      operationId: "provisionWorkspaceMember",
      method: "POST",
      path: "/access/members",
      credentials: "include",
      body,
      ...(options.signal === undefined ? {} : { signal: options.signal }),
      ...(options.idempotencyKey === undefined ? {} : { idempotencyKey: options.idempotencyKey }),
      ...(options.expectedVersion === undefined ? {} : { expectedVersion: options.expectedVersion }),
      retry: options.retry ?? "never",
    });
  }

  replaceAccessRole<TResponse = AccessMutationResponse, TBody extends object = ReplaceAccessRoleRequest>(roleId: EntityId, body: TBody, options: AccessGovernanceRequestOptions = {}): Promise<TResponse> {
    return this.http.request<TResponse, TBody>({
      operationId: "replaceAccessRole",
      method: "PUT",
      path: `/access/roles/${encodeURIComponent(roleId)}`,
      credentials: "include",
      body,
      ...(options.signal === undefined ? {} : { signal: options.signal }),
      ...(options.idempotencyKey === undefined ? {} : { idempotencyKey: options.idempotencyKey }),
      ...(options.expectedVersion === undefined ? {} : { expectedVersion: options.expectedVersion }),
      retry: options.retry ?? "never",
    });
  }

  replaceWorkspaceMemberAccess<TResponse = AccessMutationResponse, TBody extends object = ReplaceWorkspaceMemberAccessRequest>(membershipId: EntityId, body: TBody, options: AccessGovernanceRequestOptions = {}): Promise<TResponse> {
    return this.http.request<TResponse, TBody>({
      operationId: "replaceWorkspaceMemberAccess",
      method: "POST",
      path: `/access/members/${encodeURIComponent(membershipId)}/access`,
      credentials: "include",
      body,
      ...(options.signal === undefined ? {} : { signal: options.signal }),
      ...(options.idempotencyKey === undefined ? {} : { idempotencyKey: options.idempotencyKey }),
      ...(options.expectedVersion === undefined ? {} : { expectedVersion: options.expectedVersion }),
      retry: options.retry ?? "never",
    });
  }

  resendWorkspaceInvitation<TResponse = AccessMutationResponse, TBody extends object = WorkspaceInvitationActionRequest>(invitationId: EntityId, body: TBody, options: AccessGovernanceRequestOptions = {}): Promise<TResponse> {
    return this.http.request<TResponse, TBody>({
      operationId: "resendWorkspaceInvitation",
      method: "POST",
      path: `/access/invitations/${encodeURIComponent(invitationId)}/resend`,
      credentials: "include",
      body,
      ...(options.signal === undefined ? {} : { signal: options.signal }),
      ...(options.idempotencyKey === undefined ? {} : { idempotencyKey: options.idempotencyKey }),
      ...(options.expectedVersion === undefined ? {} : { expectedVersion: options.expectedVersion }),
      retry: options.retry ?? "never",
    });
  }

  revokeWorkspaceInvitation<TResponse = AccessMutationResponse, TBody extends object = WorkspaceInvitationActionRequest>(invitationId: EntityId, body: TBody, options: AccessGovernanceRequestOptions = {}): Promise<TResponse> {
    return this.http.request<TResponse, TBody>({
      operationId: "revokeWorkspaceInvitation",
      method: "POST",
      path: `/access/invitations/${encodeURIComponent(invitationId)}/revoke`,
      credentials: "include",
      body,
      ...(options.signal === undefined ? {} : { signal: options.signal }),
      ...(options.idempotencyKey === undefined ? {} : { idempotencyKey: options.idempotencyKey }),
      ...(options.expectedVersion === undefined ? {} : { expectedVersion: options.expectedVersion }),
      retry: options.retry ?? "never",
    });
  }

  rotateManagedMemberPassword<TResponse = AccessMutationResponse, TBody extends object = RotateManagedMemberPasswordRequest>(membershipId: EntityId, body: TBody, options: AccessGovernanceRequestOptions = {}): Promise<TResponse> {
    return this.http.request<TResponse, TBody>({
      operationId: "rotateManagedMemberPassword",
      method: "POST",
      path: `/access/members/${encodeURIComponent(membershipId)}/rotate-password`,
      credentials: "include",
      body,
      ...(options.signal === undefined ? {} : { signal: options.signal }),
      ...(options.idempotencyKey === undefined ? {} : { idempotencyKey: options.idempotencyKey }),
      ...(options.expectedVersion === undefined ? {} : { expectedVersion: options.expectedVersion }),
      retry: options.retry ?? "never",
    });
  }
}
