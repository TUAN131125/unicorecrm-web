// @generated by scripts/api/generate-openapi-client.mjs. Do not edit by hand.
// OpenAPI 3.1.0; contract 0.23.20-contract.0; SHA-256 2e1c4887cc158e806d05c49e9e109ae3034d7db1b57a6f0d6ffc1fe7d15e886d
import type { HttpClient } from "@/platform/api/client";

export const OPENAPI_CONTRACT_VERSION = "0.23.20-contract.0" as const;
export const OPENAPI_SPEC_SHA256 = "2e1c4887cc158e806d05c49e9e109ae3034d7db1b57a6f0d6ffc1fe7d15e886d" as const;
export const OPENAPI_SERVER_PATH = "/v1" as const;

export interface IdentityRequestOptions {
  signal?: AbortSignal;
  idempotencyKey?: string;
  expectedVersion?: number;
  retry?: "never" | "idempotent";
}

export type EntityId = string;

export type WorkspaceId = string;

export type IdempotencyKey = string;

export type ResourceVersion = number;

export type UtcDateTime = string;

export interface AcceptWorkspaceInvitationRequest {
  token: string;
}

export interface AuthSessionDeviceDocument {
  deviceId: EntityId;
  label: string;
  userAgent?: string;
  lastSeenAt: UtcDateTime;
}

export interface AuthSessionDocument {
  sessionId: EntityId;
  principal: AuthenticatedPrincipalDocument;
  status: IdentitySessionStatus;
  issuedAt: UtcDateTime;
  lastSeenAt: UtcDateTime;
  idleExpiresAt: UtcDateTime;
  absoluteExpiresAt: UtcDateTime;
  refreshCounter: number;
  assuranceLevel: IdentityAssuranceLevel;
  mfaVerifiedAt?: UtcDateTime;
  device: AuthSessionDeviceDocument;
  revokedAt?: UtcDateTime;
  revokeReason?: string;
}

export interface AuthenticatedPrincipalDocument {
  accountId: EntityId;
  memberId: EntityId;
  email: string;
  displayName: string;
}

export interface AuthenticatedSessionResponse {
  session: AuthSessionDocument;
  accessToken: string;
  accessTokenExpiresAt: UtcDateTime;
}

export type IdentityAccountStatus = "PENDING_VERIFICATION" | "ACTIVE" | "SUSPENDED";

export type IdentityAssuranceLevel = "AAL1" | "AAL2";

export type IdentitySessionStatus = "ACTIVE" | "EXPIRED" | "REVOKED";

export interface InvitationAcceptanceResponse {
  workspaceId: WorkspaceId;
  membershipId: EntityId;
  acceptedAt: UtcDateTime;
}

export interface MfaChallengeResponse {
  outcome: string;
  challengeId: EntityId;
  challengeExpiresAt: UtcDateTime;
}

export interface PasswordResetCompletedResponse {
  completedAt: UtcDateTime;
}

export interface PasswordResetRequest {
  email: string;
}

export interface PasswordResetRequestAcceptedResponse {
  requestId: EntityId;
  acceptedAt: UtcDateTime;
}

export interface RefreshSessionRequest {
}

export interface RegisterAccountRequest {
  email: string;
  password: string;
  displayName: string;
}

export interface ResetPasswordRequest {
  token: string;
  nextPassword: string;
}

export interface SessionRevocationResponse {
  sessionId: EntityId;
  revokedAt: UtcDateTime;
}

export interface SignInRequest {
  email: string;
  password: string;
  deviceLabel?: string;
}

export interface SignOutRequest {
  reason?: string;
}

export interface UserAccountDocument {
  accountId: EntityId;
  email: string;
  displayName: string;
  status: IdentityAccountStatus;
  emailVerifiedAt?: UtcDateTime;
  createdAt: UtcDateTime;
}

export interface VerifyEmailRequest {
  token: string;
}

export interface VerifyMfaRequest {
  code: string;
  deviceLabel?: string;
}

export interface AcceptWorkspaceInvitationQuery {
}

export interface GetCurrentSessionQuery {
}

export interface RefreshSessionQuery {
}

export interface RegisterAccountQuery {
}

export interface RequestPasswordResetQuery {
}

export interface ResetPasswordQuery {
}

export interface SignInQuery {
}

export interface SignOutQuery {
}

export interface VerifyEmailQuery {
}

export interface VerifyMfaQuery {
}

export class IdentityApiClient {
  constructor(private readonly http: HttpClient) {}

  acceptWorkspaceInvitation<TResponse = InvitationAcceptanceResponse, TBody extends object = AcceptWorkspaceInvitationRequest>(body: TBody, options: IdentityRequestOptions = {}): Promise<TResponse> {
    return this.http.request<TResponse, TBody>({
      operationId: "acceptWorkspaceInvitation",
      method: "POST",
      path: "/auth/invitations/accept",
      workspace: "none",
      credentials: "include",
      body,
      ...(options.signal === undefined ? {} : { signal: options.signal }),
      ...(options.idempotencyKey === undefined ? {} : { idempotencyKey: options.idempotencyKey }),
      ...(options.expectedVersion === undefined ? {} : { expectedVersion: options.expectedVersion }),
      retry: options.retry ?? "never",
    });
  }

  getCurrentSession<TResponse = AuthSessionDocument>(query: GetCurrentSessionQuery = {}, signal?: AbortSignal): Promise<TResponse> {
    return this.http.request<TResponse>({
      operationId: "getCurrentSession",
      method: "GET",
      path: "/auth/session",
      workspace: "none",
      credentials: "include",
      ...(signal === undefined ? {} : { signal }),
    });
  }

  refreshSession<TResponse = AuthenticatedSessionResponse, TBody extends object = RefreshSessionRequest>(body: TBody, options: IdentityRequestOptions = {}): Promise<TResponse> {
    return this.http.request<TResponse, TBody>({
      operationId: "refreshSession",
      method: "POST",
      path: "/auth/session/refresh",
      auth: "none",
      workspace: "none",
      credentials: "include",
      body,
      ...(options.signal === undefined ? {} : { signal: options.signal }),
      ...(options.idempotencyKey === undefined ? {} : { idempotencyKey: options.idempotencyKey }),
      ...(options.expectedVersion === undefined ? {} : { expectedVersion: options.expectedVersion }),
      retry: options.retry ?? "never",
    });
  }

  registerAccount<TResponse = UserAccountDocument, TBody extends object = RegisterAccountRequest>(body: TBody, options: IdentityRequestOptions = {}): Promise<TResponse> {
    return this.http.request<TResponse, TBody>({
      operationId: "registerAccount",
      method: "POST",
      path: "/auth/accounts",
      auth: "none",
      workspace: "none",
      credentials: "include",
      body,
      ...(options.signal === undefined ? {} : { signal: options.signal }),
      ...(options.idempotencyKey === undefined ? {} : { idempotencyKey: options.idempotencyKey }),
      ...(options.expectedVersion === undefined ? {} : { expectedVersion: options.expectedVersion }),
      retry: options.retry ?? "never",
    });
  }

  requestPasswordReset<TResponse = PasswordResetRequestAcceptedResponse, TBody extends object = PasswordResetRequest>(body: TBody, options: IdentityRequestOptions = {}): Promise<TResponse> {
    return this.http.request<TResponse, TBody>({
      operationId: "requestPasswordReset",
      method: "POST",
      path: "/auth/password-reset-requests",
      auth: "none",
      workspace: "none",
      credentials: "include",
      body,
      ...(options.signal === undefined ? {} : { signal: options.signal }),
      ...(options.idempotencyKey === undefined ? {} : { idempotencyKey: options.idempotencyKey }),
      ...(options.expectedVersion === undefined ? {} : { expectedVersion: options.expectedVersion }),
      retry: options.retry ?? "never",
    });
  }

  resetPassword<TResponse = PasswordResetCompletedResponse, TBody extends object = ResetPasswordRequest>(body: TBody, options: IdentityRequestOptions = {}): Promise<TResponse> {
    return this.http.request<TResponse, TBody>({
      operationId: "resetPassword",
      method: "POST",
      path: "/auth/password-resets",
      auth: "none",
      workspace: "none",
      credentials: "include",
      body,
      ...(options.signal === undefined ? {} : { signal: options.signal }),
      ...(options.idempotencyKey === undefined ? {} : { idempotencyKey: options.idempotencyKey }),
      ...(options.expectedVersion === undefined ? {} : { expectedVersion: options.expectedVersion }),
      retry: options.retry ?? "never",
    });
  }

  signIn<TResponse = AuthenticatedSessionResponse, TBody extends object = SignInRequest>(body: TBody, options: IdentityRequestOptions = {}): Promise<TResponse> {
    return this.http.request<TResponse, TBody>({
      operationId: "signIn",
      method: "POST",
      path: "/auth/sessions",
      auth: "none",
      workspace: "none",
      credentials: "include",
      body,
      ...(options.signal === undefined ? {} : { signal: options.signal }),
      ...(options.idempotencyKey === undefined ? {} : { idempotencyKey: options.idempotencyKey }),
      ...(options.expectedVersion === undefined ? {} : { expectedVersion: options.expectedVersion }),
      retry: options.retry ?? "never",
    });
  }

  signOut<TResponse = SessionRevocationResponse, TBody extends object = SignOutRequest>(body: TBody, options: IdentityRequestOptions = {}): Promise<TResponse> {
    return this.http.request<TResponse, TBody>({
      operationId: "signOut",
      method: "POST",
      path: "/auth/session/logout",
      workspace: "none",
      credentials: "include",
      body,
      ...(options.signal === undefined ? {} : { signal: options.signal }),
      ...(options.idempotencyKey === undefined ? {} : { idempotencyKey: options.idempotencyKey }),
      ...(options.expectedVersion === undefined ? {} : { expectedVersion: options.expectedVersion }),
      retry: options.retry ?? "never",
    });
  }

  verifyEmail<TResponse = UserAccountDocument, TBody extends object = VerifyEmailRequest>(body: TBody, options: IdentityRequestOptions = {}): Promise<TResponse> {
    return this.http.request<TResponse, TBody>({
      operationId: "verifyEmail",
      method: "POST",
      path: "/auth/email-verifications",
      auth: "none",
      workspace: "none",
      credentials: "include",
      body,
      ...(options.signal === undefined ? {} : { signal: options.signal }),
      ...(options.idempotencyKey === undefined ? {} : { idempotencyKey: options.idempotencyKey }),
      ...(options.expectedVersion === undefined ? {} : { expectedVersion: options.expectedVersion }),
      retry: options.retry ?? "never",
    });
  }

  verifyMfa<TResponse = AuthenticatedSessionResponse, TBody extends object = VerifyMfaRequest>(challengeId: EntityId, body: TBody, options: IdentityRequestOptions = {}): Promise<TResponse> {
    return this.http.request<TResponse, TBody>({
      operationId: "verifyMfa",
      method: "POST",
      path: `/auth/challenges/${encodeURIComponent(challengeId)}/verify`,
      auth: "none",
      workspace: "none",
      credentials: "include",
      body,
      ...(options.signal === undefined ? {} : { signal: options.signal }),
      ...(options.idempotencyKey === undefined ? {} : { idempotencyKey: options.idempotencyKey }),
      ...(options.expectedVersion === undefined ? {} : { expectedVersion: options.expectedVersion }),
      retry: options.retry ?? "never",
    });
  }
}
