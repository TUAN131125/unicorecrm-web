# Shared product patterns — reserved

These folders are intentionally empty. Do not move existing Lead, Contact, Order, or other module UI here yet.

A pattern can be implemented only after:
1. an approved UX/component contract exists;
2. at least two stable module-local implementations prove the shared shape;
3. the public API is domain-agnostic.
